org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Wersja, na której mod jest KOMPILOWANY. Nie jest to jedyna wersja, na której
# działa — mod nie ma mixinów i nie dotyka API zmiennego między wydaniami, więc
# jeden jar obsługuje cały zakres z `depends.minecraft` w fabric.mod.json.
minecraft_version=1.21.8
loader_version=0.19.3
fabric_version=0.136.1+1.21.8

mod_version=1.0.0
maven_group=dev.kajor
archives_base_name=skinlibrary
